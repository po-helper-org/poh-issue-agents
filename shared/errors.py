"""Типы отказов, по которым контур принимает разные решения.

Тип отказа — это решение о повторе, а не украшение. Temporal сверяет
`non_retryable_error_types` по ИМЕНИ типа, поэтому отказ, который повторять
можно, обязан отличаться типом от того, который повторять нельзя.
"""


class RateLimited(Exception):
    """Провайдер модели отказал по лимиту частоты.

    Не отказ стадии: к качеству прогона отношения не имеет, повтор осмыслен.
    Отдельный тип нужен затем, что стадии анализа объявлены неретраебельными
    через `RuntimeError` — прогон недетерминирован, мутирует файлы и стоит
    денег. Лимит частоты под это правило не подпадает, и на живом прогоне
    `poh-demo-checkout#165` оно списало сорок пять минут работы и пять
    готовых стадий из шести.
    """
