temporalio==1.9.0
instructor==1.7.2
openai==1.61.0
pydantic==2.10.5
requests==2.32.3
pyjwt[crypto]==2.10.1
sentry-sdk==2.20.0

# Delivery-Agent — модуль релиз-менеджера из соседнего репозитория. Пин на
# ПОЛНЫЙ SHA, а не на ветку: BuildKit кэширует git-клон по URL, и после нового
# коммита в ту же ветку сборка молча берёт прежний код (та же ловушка, что у
# ISSUE_AGENT_CONTEXT в harness/docker-compose.yml).
poh-delivery-agent @ git+https://github.com/po-helper-org/poh-delivery-agent@2ad0e3eacf4d7552ab9882c3f037d47b404b986c

# Стадия «Разработка» — чистые модули из соседнего репозитория: контракт
# прогона, разбор JUnit, круг правок, публикация рабочего дерева, состав
# `.harness/` и инварианты образа раннера.
#
# Пин на ПОЛНЫЙ SHA по той же причине, что и у соседей. Замысел переноса
# (po-helper-org/poh-developer-agents, docs/design/2026-09-05-developer-as-a-service-design.md)
# требовал тега, но пуш тегов из среды, где ведётся перенос, отклоняется
# шлюзом с 403 — SHA выбран осознанно как замена, а не по забывчивости.
#
# Из пакета контур берёт стадию целиком: чистые модули, шаги, воркфлоу и типы,
# которые едут через границу репозиториев. Разговор со стадией идёт через порты,
# реализации — в `worker/developer_bridge.py`; исполняется она на своей очереди
# (`developer`), которую поднимает `worker/worker.py`.
poh-developer @ git+https://github.com/po-helper-org/poh-developer-agents@ba44e277556604641166c32cbcc9b1f500ddef83

# HowToDemo-Agent — модуль приёмки из соседнего репозитория. Пин на ПОЛНЫЙ
# SHA по той же причине, что и у Delivery-Agent выше.
poh-howtodemo-agent @ git+https://github.com/po-helper-org/poh-howtodemo-agent@cfc3b620b8acba2f80f1fe33ab91a4d12b4c50b1
