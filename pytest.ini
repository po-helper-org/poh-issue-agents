[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
timeout = 120
testpaths = tests
# Покрытие считается при каждом прогоне, а не разово: цифра, которую никто не
# видит, не защищает ни от чего. Порог и исключения — в .coveragerc.
addopts = --cov --cov-report=term-missing
