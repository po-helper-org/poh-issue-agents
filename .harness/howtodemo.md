**Было раньше:** стадия получает `429`, поднимает `RuntimeError`, попытка признаётся неповторяемой, анализ падает целиком, работа пяти стадий списывается в частичную выкладку.

**Как должно работать:** стадия получает `429`, отказ опознан как временный, попытка повторяется с отступом, анализ доходит до конца. Отказ самой стадии — испорченный артефакт, отсутствующий вход, ошибка в промпте — по-прежнему неповторяем и доезжает до человека без повторов.