было — `grep -n "and workflow.patched\|or workflow.patched" worker/workflows.py` находит десяток мест, где маркер вычисляется вторым операндом, а таблица в AGENTS.md перечисляет 9 маркеров из 40; стало — grep не находит ни одного такого места, а число строк таблицы совпадает с числом маркеров в коде и держится тестом.